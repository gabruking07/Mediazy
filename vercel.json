{
  "framework": "nextjs",
  "installCommand": "if [ -f client/package.json ]; then npm install --prefix client; else npm install; fi",
  "buildCommand": "if [ -f client/package.json ]; then npm run build --prefix client; else npm run build; fi"
}
